import styled from 'styled-components'

export const MainStyled = styled.main`
  display: flex;
  flex-direction: column;
  align-items: center;
  margin: 4rem 2.5rem;
  > p {
    font-weight: 400;
    font-size: 16px;
    line-height: 24px;
  }
`
