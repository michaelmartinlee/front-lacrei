import styled from 'styled-components'

export const StatesSelectStyled = styled.div`
  display: flex;
  flex-direction: column;
  > label {
    margin-top: 3rem;
  }
  > select {
    margin-top: 3rem;
    height: 50px;
    border-radius: 8px;
    padding: 5px;
  }
`
