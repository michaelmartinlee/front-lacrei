import styled from 'styled-components'

export const StatesSelectStyled = styled.div`
  display: flex;
  flex-direction: column;
  > label {
    margin-top: 3rem;
  }
  > select {
    margin: 3rem 0;
    height: 50px;
    border-radius: 8px;
    padding: 5px;
  }
`
